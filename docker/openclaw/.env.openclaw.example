# =============================================================================  
# OpenClaw Docker 环境变量模板  
# 复制为 .env，填写需要的值，其余保持注释状态  
# =============================================================================  
  
# -----------------------------------------------------------------------------  
# 镜像  
# -----------------------------------------------------------------------------  
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest  
  
# -----------------------------------------------------------------------------  
# Gateway 认证（必填其一）  
# 留空则首次启动自动生成 token；手动生成：openssl rand -hex 32  
# 不要使用文档示例值  
# -----------------------------------------------------------------------------  
OPENCLAW_GATEWAY_TOKEN=  
# OPENCLAW_GATEWAY_PASSWORD=  
  
# -----------------------------------------------------------------------------  
# 宿主机挂载路径（可选，有默认值）  
# -----------------------------------------------------------------------------  
OPENCLAW_CONFIG_DIR=/opt/openclaw/config  
OPENCLAW_WORKSPACE_DIR=/opt/openclaw/workspace  
OPENCLAW_AUTH_PROFILE_SECRET_DIR=/opt/openclaw/auth-secrets  
  
# -----------------------------------------------------------------------------  
# 端口映射（可选，有默认值）  
# -----------------------------------------------------------------------------  
OPENCLAW_GATEWAY_PORT=18789  
OPENCLAW_BRIDGE_PORT=18790  
OPENCLAW_MSTEAMS_PORT=3978  
  
# -----------------------------------------------------------------------------  
# 网络与行为  
# -----------------------------------------------------------------------------  
# lan = 0.0.0.0（Docker 端口映射必须用 lan）；loopback = 127.0.0.1  
OPENCLAW_GATEWAY_BIND=lan  
# 容器内 bridge 网络不支持 mDNS，建议强制关闭  
OPENCLAW_DISABLE_BONJOUR=1  
OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=  
